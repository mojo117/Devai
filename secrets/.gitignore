# Plaintext secrets - NEVER commit
templates/devai.env

# age recipients file (contains public keys, but keep private)
.age-recipients

# Local age keys (should never be here, but just in case)
*.txt
keys/
